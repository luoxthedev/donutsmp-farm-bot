module.exports = {
  // Host, port and version configuration shared by all bots.
  server: {
    // Change these values to match the server you want the bots to connect to
    host: 'donutsmp.net',
    port: 25565,
    version: '1.20.4'
  }
};